export const translateToIDF = async (input: string): Promise<string> => {
    return `Mock IDF file content for input: ${input}`;
  };
  
  export const translateToNaturalLanguage = async (input: string): Promise<string> => {
    return `Mock natural language description for IDF content: ${input}`;
  };